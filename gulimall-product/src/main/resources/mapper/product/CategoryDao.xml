<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.laughingather.gulimall.product.dao.CategoryDao">
    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.laughingather.gulimall.product.entity.CategoryEntity" id="categoryMap">
        <result property="categoryId" column="category_id"/>
        <result property="categoryName" column="category_name"/>
        <result property="parentCid" column="parent_cid"/>
        <result property="categoryLevel" column="category_level"/>
        <result property="showStatus" column="show_status"/>
        <result property="sort" column="sort"/>
        <result property="icon" column="icon"/>
        <result property="productUnit" column="product_unit"/>
        <result property="productCount" column="product_count"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectListWithTree" resultType="com.laughingather.gulimall.product.entity.vo.CategoryTreeVO">
        SELECT category_id,
               category_name,
               parent_cid,
               category_level,
               show_status,
               sort,
               icon,
               product_unit,
               product_count
        FROM pms_category
        WHERE show_status = 1;
    </select>
</mapper>