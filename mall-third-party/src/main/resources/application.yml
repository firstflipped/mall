server:
  port: 99
  servlet:
    context-path: /mall-third-party

spring:
  profiles:
    active: dev

# 监控信息
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always