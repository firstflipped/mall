spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS.SERVER-ADDR:http://nacos-registry:8848}
        namespace: ${NACOS.NAMESPACE:mall}
      config:
        server-addr: ${NACOS.SERVER-ADDR:http://nacos-registry:8848}
        namespace: ${NACOS.NAMESPACE:mall}
        file-extension: yaml