spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS.SERVER-ADDR:http://nacos-registry:8848}
        namespace: ${NACOS.NAMESPACE:gulimall}
      config:
        server-addr: ${NACOS.SERVER-ADDR:http://nacos-registry:8848}
        namespace: ${NACOS.NAMESPACE:gulimall}
        file-extension: yaml