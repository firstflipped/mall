server:
  port: 8087
  servlet:
    context-path: /mall-seckill

spring:
  profiles:
    active: dev

  main:
    allow-bean-definition-overriding: true

  task:
    execution:
      pool:
        core-size: 20
        max-size: 200

