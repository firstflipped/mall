spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://nacos-registry:8848
        namespace: mall
      config:
        server-addr: http://nacos-registry:8848
        namespace: mall
        file-extension: yaml